# Scenario 02 — Axios 呼叫與 API 契約不一致

## 背景

前端以 Axios 呼叫後端保單 API，但欄位名稱與錯誤格式和 OpenAPI 文件不一致，導致畫面顯示失敗。團隊成員彼此說法不同，卻沒有共同依據。

## 任務

1. 找出前端假設與 API 契約不一致之處。
2. 決定應修正前端還是更新 API 文件。
3. 說明為什麼專題一開始就要先約定 API 規格。